const URL='https://wfiuqoiycnftzhimewri.supabase.co', KEY='sb_publishable_9F4HiApJgfZB3pMS0J9cPg_PA0cH37U';
const client=window.supabase.createClient(URL,KEY,{auth:{persistSession:true,autoRefreshToken:true,detectSessionInUrl:true}}); let rows=[], selected=null, action='receive', receiveQueue=[], receiveItems=[], reorderAlerts=[], reorderMap={};
const money=v=>new Intl.NumberFormat('pt-BR',{style:'currency',currency:'BRL'}).format(Number(v||0));
async function start(){const {data:{session}}=await client.auth.getSession(); if(!session) return renderLogin(); const {data:admin}=await client.from('app_admins').select('display_name,role,is_active').eq('email',session.user.email).maybeSingle(); if(!admin?.is_active){await client.auth.signOut();return renderLogin('Acesso não autorizado.')} renderApp(session,admin); await load();}
function renderLogin(msg=''){root.innerHTML=`<div class="login"><h2>LUMI Control Center</h2><p>Área administrativa de estoque</p>${msg?`<div class="msg">${msg}</div>`:''}<label>E-mail<input id="email" type="email"></label><label>Senha<input id="pass" type="password"></label><button class="btn primary" style="width:100%" onclick="login()">Entrar</button></div>`}
async function login(){const {error}=await client.auth.signInWithPassword({email:email.value,password:pass.value}); if(error)return renderLogin(error.message==='Invalid login credentials'?'E-mail ou senha incorretos.':error.message);start()}
function renderApp(session,admin){root.innerHTML=`<div class="shell"><aside class="side"><div class="logo">◇ LUMI<small>CONTROL CENTER</small></div><nav class="nav"><a href="./">Visão geral</a><a href="./">Produtos</a><a href="./">Compras</a><a class="active">Estoque</a><a href="./">Preço & margem</a><a href="./">Radar de fornecedores</a></nav><div class="who"><strong>${admin.display_name} · ADMIN</strong><small>${session.user.email}</small><button class="btn" style="margin-top:10px" onclick="logout()">Sair</button></div></aside><main class="main"><header class="top"><div><h1>Estoque</h1><p>Central operacional: receber, conferir, repor e movimentar.</p></div><button class="btn" onclick="load()">↻ Atualizar</button></header><div id="content"></div></main></div>`}
async function load(){content.innerHTML='<div class="msg">Atualizando operação...</div>';const [r0,h0,q0,i0,a0,m0]=await Promise.all([client.from('v_product_performance').select('*').order('name'),client.from('inventory_adjustments').select('id,adjustment_type,quantity,qty_before,qty_after,note,actor_email,created_at,products(name,sku)').order('created_at',{ascending:false}).limit(40),client.from('v_inventory_receiving_queue').select('*').order('purchase_date',{ascending:true}),client.from('v_inventory_receiving_items').select('*').order('name'),client.from('v_inventory_reorder_alerts').select('*').order('name'),client.from('inventory').select('product_id,reorder_point')]);if(r0.error){content.innerHTML=`<div class="msg">${r0.error.message}</div>`;return}rows=r0.data||[];receiveQueue=q0.data||[];receiveItems=i0.data||[];reorderAlerts=a0.data||[];reorderMap=Object.fromEntries((m0.data||[]).map(x=>[x.product_id,Number(x.reorder_point||0)]));renderStock(h0.data||[])}
function renderStock(history){const on=rows.reduce((a,b)=>a+b.qty_on_hand,0),av=rows.reduce((a,b)=>a+b.qty_available,0),res=rows.reduce((a,b)=>a+b.qty_reserved,0),pending=receiveQueue.reduce((a,b)=>a+Number(b.units_pending||0),0),val=rows.reduce((a,b)=>a+b.qty_on_hand*Number(b.avg_landed_cost_brl),0),unconfigured=rows.filter(x=>x.qty_on_hand>0 && Number(reorderMap[x.product_id]||0)===0).length,configuredAlerts=reorderAlerts.filter(x=>Number(x.reorder_point||0)>0),attention=pending+configuredAlerts.length+(unconfigured?1:0);content.innerHTML=`<div class="cards"><div class="card"><small>Em mãos</small><strong>${on} un.</strong></div><div class="card"><small>Disponíveis</small><strong>${av} un.</strong></div><div class="card"><small>Reservadas</small><strong>${res} un.</strong></div><div class="card"><small>Para receber</small><strong>${pending} un.</strong></div><div class="card"><small>Capital no estoque</small><strong>${money(val)}</strong></div></div>${operations(attention,pending,configuredAlerts.length,unconfigured,res)}<section class="hero"><div><h2>Estoque sem complicação.</h2><p>Registre o que aconteceu fisicamente. O sistema atualiza as quantidades e guarda quem fez cada movimentação.</p></div><button class="btn" onclick="document.getElementById('q').focus()">Buscar produto</button></section>${receivingSection()}${reorderSection()}<section class="panel"><div class="toolbar"><div><strong>Produtos e disponibilidade</strong><div style="color:#747c75;font-size:11px;margin-top:4px">Clique em Movimentar para entrada, reserva, baixa, avaria ou contagem</div></div><input id="q" class="search" placeholder="Buscar produto ou SKU" oninput="filterRows()"></div><div class="head"><span>Produto</span><span>Em mãos</span><span>Reservado</span><span>Disponível</span><span>Custo médio</span><span>Valor</span><span></span></div><div id="table">${stockRows(rows)}</div></section><section class="panel history"><div class="toolbar"><strong>Últimas movimentações</strong><span style="color:#747c75;font-size:11px">Auditoria por usuário</span></div><div class="history-list">${history.map(x=>`<div class="hist"><div><strong>${x.products?.name||'Produto'}</strong><small>${x.products?.sku||''}</small></div><span>${label(x.adjustment_type)}</span><strong>${x.quantity}</strong><div><span>${x.actor_email||'sistema'}</span><small>${new Date(x.created_at).toLocaleString('pt-BR')}</small></div></div>`).join('')||'<div class="empty">Nenhuma movimentação manual ainda.</div>'}</div></section>`}
function operations(attention,pending,low,unconfigured,reserved){const tasks=[];if(pending>0)tasks.push(`<div class="task"><i class="dot red"></i><div><strong>Conferir ${pending} un. que estão para chegar</strong><small>Abra “Receber compras” e dê entrada somente no que chegou fisicamente.</small></div></div>`);if(low>0)tasks.push(`<div class="task"><i class="dot red"></i><div><strong>${low} produto(s) no ponto de reposição</strong><small>Veja a lista de Reposição e programe nova compra.</small></div></div>`);if(unconfigured>0)tasks.push(`<div class="task"><i class="dot"></i><div><strong>Definir mínimo de ${unconfigured} produto(s)</strong><small>Faça uma vez e o alerta de reposição passa a funcionar sozinho.</small></div></div>`);if(reserved>0)tasks.push(`<div class="task"><i class="dot green"></i><div><strong>${reserved} un. reservada(s)</strong><small>Acompanhe as entregas ou libere reservas canceladas.</small></div></div>`);if(!tasks.length)tasks.push(`<div class="task"><i class="dot green"></i><div><strong>Operação em dia</strong><small>Nenhuma ação urgente de estoque agora.</small></div></div>`);return `<section class="ops"><div class="op today"><small>ROTINA DE HOJE</small><h3>O que precisa ser feito</h3><div class="metric">${attention}</div><p>pendência(s) operacional(is) agrupadas para facilitar o dia.</p><div class="task-list">${tasks.join('')}</div></div><div class="op"><small>RECEBIMENTO</small><h3>Receber compras</h3><div class="metric">${pending} un.</div><p>Conferência ligada aos pedidos pagos/em trânsito. Aceita recebimento parcial.</p><div style="margin-top:13px"><button class="btn gold" onclick="go('receber')">Abrir conferência</button></div></div><div class="op"><small>REPOSIÇÃO</small><h3>Evitar ruptura</h3><div class="metric">${low}</div><p>${unconfigured?`${unconfigured} produto(s) ainda precisam de mínimo configurado.`:'Pontos mínimos configurados.'}</p><div style="margin-top:13px"><button class="btn" onclick="go('reposicao')">Ver reposição</button></div></div></section>`}
function go(id){document.getElementById(id)?.scrollIntoView({behavior:'smooth',block:'start'})}
function receivingSection(){return `<section id="receber" class="panel anchor"><div class="toolbar section-title"><div><strong>Receber compras</strong><div style="color:#747c75;font-size:11px;margin-top:4px">Somente compras pagas ou em trânsito aparecem aqui</div></div><span class="badge">${receiveQueue.reduce((a,b)=>a+Number(b.units_pending||0),0)} un. pendentes</span></div>${receiveQueue.length?receiveQueue.map(p=>`<div class="receive-card"><div><strong>${p.external_reference||'Compra'}</strong><small>${p.supplier||'Fornecedor'} · ${new Date(p.purchase_date+'T12:00:00').toLocaleDateString('pt-BR')}</small></div><div><strong>${p.item_lines} item(ns)</strong><small>${p.units_received}/${p.units_ordered} recebidas</small></div><div><span class="badge red">${p.units_pending} pend.</span></div><div>${money(p.total_paid_brl)}</div><button class="btn primary" onclick="openReceive('${p.purchase_id}')">Conferir</button></div>`).join(''):'<div class="empty">✓ Nenhuma compra aguardando recebimento.</div>'}</section>`}
function reorderSection(){const list=rows.filter(x=>x.qty_on_hand>0).sort((a,b)=>{const ar=Number(reorderMap[a.product_id]||0),br=Number(reorderMap[b.product_id]||0);const aa=ar>0&&xAvail(a)<=ar?0:ar===0?1:2,bb=br>0&&xAvail(b)<=br?0:br===0?1:2;return aa-bb||a.name.localeCompare(b.name)});return `<section id="reposicao" class="panel anchor"><div class="toolbar section-title"><div><strong>Reposição</strong><div style="color:#747c75;font-size:11px;margin-top:4px">Defina o mínimo de cada SKU. Depois o sistema avisa automaticamente.</div></div><span class="badge good">${reorderAlerts.filter(x=>Number(x.reorder_point||0)>0).length} alerta(s)</span></div><div class="reorder-grid">${list.map(x=>reorderItem(x)).join('')||'<div class="empty">Sem produtos em estoque.</div>'}</div></section>`}
function xAvail(x){return Number(x.qty_available||0)}
function reorderItem(x){const rp=Number(reorderMap[x.product_id]||0),avail=xAvail(x),level=rp>0&&avail===0?'red':rp>0&&avail<=rp?'':'good',status=rp===0?'Sem mínimo':avail===0?'ESGOTADO':avail<=rp?'REPOR':'OK';return `<div class="reorder-item"><div class="line"><div><strong>${x.name}</strong><small>${x.sku} · disponível ${avail} un.</small></div><span class="badge ${level}">${status}</span></div><div class="reorder-controls"><span>Mínimo:</span><input id="rp_${x.product_id}" type="number" min="0" value="${rp}" aria-label="Ponto mínimo"><button class="btn small" onclick="saveReorder('${x.product_id}')">Salvar</button></div></div>`}
async function saveReorder(productId){const el=document.getElementById('rp_'+productId),n=Math.max(0,Number(el.value)||0);const {error}=await client.rpc('inventory_set_reorder_point',{p_product_id:productId,p_reorder_point:n});if(error)return alert(error.message);await load();setTimeout(()=>go('reposicao'),50)}
function stockRows(list){return list.map(x=>{const rp=Number(reorderMap[x.product_id]||0),isLow=rp>0&&x.qty_available<=rp;return `<div class="row ${isLow?'low':''}"><div class="prod"><strong>${x.name}${isLow?' · repor':''}</strong><small>${x.sku} · ${x.category}${rp?` · mínimo ${rp}`:''}</small></div><strong>${x.qty_on_hand}</strong><span>${x.qty_reserved}</span><strong>${x.qty_available}</strong><span>${money(x.avg_landed_cost_brl)}</span><strong>${money(x.qty_on_hand*x.avg_landed_cost_brl)}</strong><button class="btn" onclick="openMove('${x.product_id}')">Movimentar</button></div>`}).join('')}
function filterRows(){const s=q.value.toLowerCase();table.innerHTML=stockRows(rows.filter(x=>(x.name+' '+x.sku+' '+x.category).toLowerCase().includes(s)))}
function label(a){return({receive:'Entrada',set_count:'Contagem',reserve:'Reserva',release:'Liberação',sale:'Baixa/Venda',damage:'Avaria',return:'Devolução',correction:'Correção'})[a]||a}
function openReceive(purchaseId){const p=receiveQueue.find(x=>x.purchase_id===purchaseId),items=receiveItems.filter(x=>x.purchase_id===purchaseId);modal.className='modal show';modal.innerHTML=`<div class="box"><div class="boxhead"><div><small>Conferência de compra</small><h3>${p?.external_reference||'Compra'}</h3><span>${p?.supplier||''} · informe somente o que chegou fisicamente</span></div><button class="btn" onclick="closeMove()">✕</button></div><div style="margin-top:14px">${items.map(it=>`<div class="receive-line"><div><strong>${it.name}</strong><small>${it.sku} · pedido ${it.quantity_ordered}, já recebido ${it.quantity_received}</small></div><input id="recv_${it.purchase_item_id}" type="number" min="0" max="${it.quantity_pending}" value="${it.quantity_pending}"><span>máx. ${it.quantity_pending}</span></div>`).join('')}</div><label class="field">Observação<textarea id="receive_note" placeholder="Ex.: caixa conferida, faltou 1 unidade, embalagem avariada..."></textarea></label><div id="fmsg"></div><div class="foot"><button class="btn" onclick="closeMove()">Cancelar</button><button class="btn primary" onclick="confirmReceive('${purchaseId}')">Confirmar recebimento</button></div></div>`}
async function confirmReceive(purchaseId){const items=receiveItems.filter(x=>x.purchase_id===purchaseId);let sent=0;fmsg.innerHTML='<div class="msg">Registrando recebimento...</div>';for(const it of items){const q=Math.max(0,Number(document.getElementById('recv_'+it.purchase_item_id).value)||0);if(!q)continue;if(q>Number(it.quantity_pending)){fmsg.innerHTML='<div class="msg">Quantidade maior que o pendente.</div>';return}const {error}=await client.rpc('inventory_receive_purchase_item',{p_purchase_item_id:it.purchase_item_id,p_quantity:q,p_note:receive_note.value||null});if(error){fmsg.innerHTML=`<div class="msg">${error.message}</div>`;return}sent+=q}if(!sent){fmsg.innerHTML='<div class="msg">Informe ao menos uma unidade recebida.</div>';return}fmsg.innerHTML=`<div class="msg">✓ ${sent} unidade(s) recebida(s) e estoque atualizado.</div>`;setTimeout(async()=>{closeMove();await load();go('receber')},700)}
function openMove(id){selected=rows.find(x=>x.product_id===id);action='receive';modal.className='modal show';modal.innerHTML=`<div class="box"><div class="boxhead"><div><small>Movimentação de estoque</small><h3>${selected.name}</h3><span>${selected.sku} · Em mãos ${selected.qty_on_hand} · Disponível ${selected.qty_available}</span></div><button class="btn" onclick="closeMove()">✕</button></div><div class="actions" id="acts">${['receive','set_count','reserve','release','sale','damage','return'].map(a=>`<button class="${a===action?'active':''}" onclick="setAction('${a}')">${label(a)}</button>`).join('')}</div><label class="field">Quantidade<input id="qty" type="number" min="0" value="1"></label><label class="field">Observação<textarea id="note" placeholder="Ex.: conferência física, cliente, motivo da avaria..."></textarea></label><div id="fmsg"></div><div class="foot"><button class="btn" onclick="closeMove()">Cancelar</button><button class="btn primary" onclick="applyMove()">Confirmar movimentação</button></div></div>`}
function setAction(a){const id=selected.product_id;action=a;openMove(id);action=a;document.querySelectorAll('#acts button').forEach(b=>b.classList.toggle('active',b.textContent===label(a)));document.getElementById('qty').value=1}
function closeMove(){modal.className='modal';modal.innerHTML='';selected=null}
async function applyMove(){const quantity=Math.max(0,Number(qty.value)||0);const {error}=await client.rpc('inventory_apply_adjustment',{p_product_id:selected.product_id,p_type:action,p_quantity:quantity,p_note:note.value||null});if(error){fmsg.innerHTML=`<div class="msg">${error.message}</div>`;return}fmsg.innerHTML='<div class="msg">Estoque atualizado com sucesso.</div>';setTimeout(async()=>{closeMove();await load()},500)}
async function logout(){await client.auth.signOut();renderLogin()}
start();